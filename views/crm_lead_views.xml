<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend CRM Lead Form View - Add Check-in Button to Header -->
    <record id="crm_lead_view_form_inherit_visit_tracking" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.visit.tracking</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <!-- Add Check-in button before Convert to Opportunity button -->
            <xpath expr="//button[@name='%(crm.action_crm_lead2opportunity_partner)d']" position="before">
                <div class="o_lead_geolocation_control d-inline-block me-2">
                    <widget name="lead_geolocation_button"/>
                </div>
            </xpath>

            <!-- Add Visit Count stat button in button_box -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_visits" type="object"
                    class="oe_stat_button" icon="fa-map-marker"
                    invisible="visit_count == 0">
                    <div class="o_stat_info">
                        <field name="visit_count" class="o_stat_value"/>
                        <span class="o_stat_text">Visits</span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
